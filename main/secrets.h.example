#pragma once

/* Redes WiFi conocidas. El firmware escanea al arrancar y se conecta
 * a la primera de esta lista que esté disponible (orden = prioridad). */
#define WIFI_AP_LIST { \
    { "TU_SSID_1", "TU_PASSWORD_1" }, \
    { "TU_SSID_2", "TU_PASSWORD_2" }  \
}

/* AWS IoT Core — específico de tu cuenta (no versionar).
 * Endpoint: aws iot describe-endpoint --endpoint-type iot:Data-ATS  */
#define AWS_IOT_ENDPOINT  "xxxxxxxxxxxxx-ats.iot.us-east-2.amazonaws.com"
#define AWS_THING_NAME    "tu_objeto_iot"
